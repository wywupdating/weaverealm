# 织境架构重构决策（06-08）

## 背景
织境项目已在GitHub建仓（wywupdating/weaverealm），初始版本包含 docs/foundational.md + docs/creators-guide.md + stories/ 目录，定位为"共创小说项目"。06-08修正定位为"底层架构实验"后，需要调整仓库结构。

## 用户指令
大白明确：
1. **砍掉**：暗线、长篇、三层内容架构（核心/中间/外层）、复杂共创机制
2. **聚焦**：搭建完整、正确的底层架构（理论骨架）
3. **架构独立**：架构本身要有架构应该有的结构和形态，不依赖故事
4. **故事是可选载体**：放在独立文件夹，开头用 `architecture:` 元数据标注引用哪些架构模块（可以一篇对应多个模块）

## 执行结果
- 新仓库结构：`architecture/`（核心）+ `stories/`（可选）+ `references/`（素材）
- 17个文件已创建（本地 `项目/织境-weaverealm/`）
- 架构全景（00-overview）：4层（基础→智能→社会→应用）+ 4条设计原则（模块自洽/单向依赖/理论先行/可证伪）
- 每个模块严格7段式：核心问题→前提假设→核心论点→理论论证→模块关系→开放问题→可证伪条件
- P0缺口（11-lifecycle、12-governance）已从零补全：
  - 11：五阶段模型+各阶段经济/治理/风险+遗产机制+清算流程
  - 12：三层治理架构（规范→调解→仲裁）+同伴调解庭+织境仲裁庭+长老会元治理
- 三个子会话并行生成（基础层/智能层/社会应用层），质量已审查

## 已完成
- ✅ push到GitHub完成（commit c507e96）：18文件通过GitHub API推送（桌面端无git/Xcode CLI），docs/旧文件已删除，raw/保留
- 三层内容架构决策（`starchronicles_three_layer_20260607.md`）已被本决策替代
